using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotateObject
{
    
    
    IEnumerator RotateCoroutine(float time, float angle, bool clockWise = true)
    {
        Vector3 wise;
        if (clockWise == true) wise = Vector3.up;
        else wise = Vector3.down;
        
        
    }
}
